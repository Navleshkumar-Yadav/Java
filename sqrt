class Solution {
    public int mySqrt(int x) {
        return (int)(Math.sqrt(x));
        // long l =0 , h = x;
        // while(l<=h){
        //     long mid = l+(h-l)/2;
        //     if(mid*mid==(long(x)) return (long)) mid;
        //     else if(mid*mid>(long(x)) h = mid-1;
        //     else{
        //         low = mid+1;
        //     }
        // }
        // return (int) h;
        
    }
}
